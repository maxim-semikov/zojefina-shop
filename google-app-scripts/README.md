# Google Apps Script

Скрипты для автоматизации обработки заказов магазина [Zojefina](https://zojefina.shop).

## Проекты

### 📦 [orders-processing](orders-processing/)

Обработка заказов, создание планов готовки и формирование счетов:

- `importOrders.js` - импорт новых заказов из источника в текущую таблицу с валидацией
- `fillDeliveryDates.js` - автоматическое заполнение дат доставки для заказов (все или по номеру)
- `cleanupImportedOrders.js` - удаление импортированных заказов из исходной таблицы
- `buildCookingPlanByDate.js` - построение плана готовки по датам доставки
- `invoice.js` - генерация накладных для заказов с группировкой по датам доставки
- `main.js` - основной файл с меню и функциями
- `utils.js` - вспомогательные функции

### 🔗 [tilda-webhook-import-orders](tilda-webhook-import-orders/)

Импорт заказов из Tilda через webhook в Google Sheets:

- `main.js` - обработка webhook от Tilda
- `config.js` - конфигурация проекта
- `token.js` - работа с токенами
- `protectRawSheet.js` - защита листа с сырыми данными
- `utils.js` - вспомогательные функции
- `appsscript.json` - манифест проекта

## Возможности

### Обработка заказов (orders-processing)

- **Импорт заказов** - автоматический импорт новых заказов из исходной таблицы с валидацией данных и отметкой статуса
- **Автозаполнение дат доставки** - вычисление дат доставки на основе дня приёма и типа доставки, с подсветкой автозаполненных ячеек
- **План приготовления** - агрегация блюд по датам доставки с визуальным форматированием и выделением заказов без даты
- **Генерация накладных** - создание детализированных накладных по номеру заказа с группировкой по датам доставки, включая информацию о скидках и калориях
- **Очистка импортированных данных** - удаление уже обработанных заказов из исходной таблицы

### Приём заказов (tilda-webhook-import-orders)

- **Webhook для Tilda** - автоматический приём заказов из Tilda CMS через POST-запросы
- **Дублирование данных** - параллельное сохранение в рабочий лист (Orders) и лист сырых данных (Raw)
- **Цветовая маркировка** - автоматическое окрашивание строк заказа для визуального разделения
- **Защита данных** - автоматическая защита листа с сырыми данными от случайных изменений
- **Валидация токенов** - проверка подлинности запросов через секретный токен

## Работа с проектами

### Первоначальная настройка

1. **Авторизация в Google**

   ```bash
   clasp login
   ```

2. **Создание конфигураций**

   Для каждого проекта нужен файл `.clasp.json` с Script ID из Google Apps Script.

   **Как найти Script ID:**
   - Откройте проект в [Google Apps Script](https://script.google.com)
   - Нажмите на ⚙️ "Project Settings"
   - Скопируйте "Script ID"

   **Создайте `.clasp.json`** в директории каждого проекта:

   ```json
   {
     "scriptId": "ВАШ_SCRIPT_ID",
     "rootDir": "."
   }
   ```

### Основные команды

#### Из корня проекта (используя helper-скрипт):

```bash
# Отправить изменения в Google
./deploy.sh orders-processing push
./deploy.sh tilda-webhook push

# Получить изменения из Google
./deploy.sh orders-processing pull
./deploy.sh tilda-webhook pull

# Открыть проект в браузере
./deploy.sh orders-processing open
./deploy.sh tilda-webhook open
```

#### Напрямую (из директории проекта):

```bash
cd orders-processing  # или tilda-webhook-import-orders

# Отправить изменения в Google Apps Script
clasp push

# Скачать изменения из Google Apps Script
clasp pull

# Открыть проект в браузере
clasp open

# Посмотреть статус
clasp status

# Автоматическая отправка при изменениях
clasp push --watch
```

### Рабочий процесс

1. **Перед началом работы:** `clasp pull` - получить последние изменения
2. **Редактируйте файлы** локально в VS Code
3. **Отправьте изменения:** `clasp push` - загрузить в Google
4. **Проверьте** в Google Apps Script, что всё работает

### Важные замечания

- `.clasp.json` добавлен в `.gitignore` и не коммитится (содержит приватный Script ID)
- `appsscript.json` - манифест проекта, синхронизируется автоматически
- При первом `clasp pull` могут скачаться дополнительные файлы из Google

### Решение проблем

**Ошибка при push/pull:**

```bash
clasp logout
clasp login
```

**Пересоздать конфигурацию:**

```bash
clasp clone YOUR_SCRIPT_ID
```

## Полезные ссылки

- [Документация clasp](https://github.com/google/clasp)
- [Google Apps Script](https://script.google.com)
- [Google Apps Script API](https://developers.google.com/apps-script/api)
